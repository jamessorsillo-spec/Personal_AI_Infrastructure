{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",

  "_comment": "Project-level settings for PAI cross-device sync. These hooks ensure .claude/ config (CLAUDE.md, context, hooks) stays in sync across Mac, web, and mobile sessions via git. Device-specific config (MCP auth tokens, Kitty hooks, voice) stays in user-level ~/.claude/settings.json.",

  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/session-start-sync.sh"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "bash .claude/hooks/session-stop-sync.sh"
          }
        ]
      }
    ]
  }
}
